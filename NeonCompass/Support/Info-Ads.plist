<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Valeur fournie par configuration (project.yml → settings.configs), parce
	     qu'un fichier de propriétés ne peut pas porter de `#if DEBUG`. Voir
	     Core/Ads/AdUnits.swift pour la même séparation côté unités : l'App ID et
	     les unités doivent rester cohérents dans une même configuration — un App
	     ID de test avec une unité réelle ne sert rien et n'échoue pas bruyamment. -->
	<key>GADApplicationIdentifier</key>
	<string>$(GAD_APP_ID)</string>

	<!-- Schéma de retour du flux OAuth Google. Doit rester identique à
	     `SupabaseAuthProvider.oauthRedirectURL` ET figurer dans les URL de
	     redirection autorisées du projet Supabase : si l'une des trois manque,
	     le navigateur reste ouvert sur une page blanche, sans erreur. -->
	<!-- Sans cette clé, `Activity.request` lève `ActivityAuthorizationError
	     .attributesTooLarge`… non : elle lève `.unsupported`, et surtout
	     `ActivityAuthorizationInfo().areActivitiesEnabled` reste FAUX quoi que
	     l'utilisateur autorise dans Réglages. Elle n'a pas de mapping
	     INFOPLIST_KEY_* reconnu, d'où sa place dans ce fichier partiel. -->
	<key>NSSupportsLiveActivities</key>
	<true/>

	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>co.antoineteston.neoncompass.auth</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>co.antoineteston.neoncompass</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
