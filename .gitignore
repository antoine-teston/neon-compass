.DS_Store
xcuserdata/
DerivedData/
*.xcodeproj
.build/
tools/content-cli/dist/
# Cache Deno des Edge Functions
supabase/functions/**/deno.lock

# Secrets d'environnement. Le moniteur du Pi lit `tools/monitor/.env`, qui
# contient MONITOR_TOKEN — un fichier de secrets non ignoré est un secret
# committé en une seule distraction.
.env
.env.*
!.env.example
tools/basemap/.cache/
