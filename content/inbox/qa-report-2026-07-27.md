# Rapport QA contenu — 2026-07-27

Commit couvert : `e8f33ce` (« content: veille 2026-07-27 — scout facts (régions
Leonida + actu) + 5 POI drafts »).
Fichiers contrôlés : 5 drafts POI + inbox `2026-07-27-gta6-map-regions.facts.json`
et `2026-07-27-gta6-news-cheats.facts.json` + `content/inbox/runs/2026-07-27.md`.
Validation programmatique : `jsonschema` (Python) contre `content/schema/poi.schema.json`.

**Limitation du run (récurrente)** : WebFetch direct vers `gta.fandom.com` reste
bloqué en `403 Forbidden` via le proxy d'egress — retesté ce run sur
`https://gta.fandom.com/wiki/Ambrosia` et `https://gta.fandom.com/wiki/Port_Gellhorn`,
même blocage que les runs du 21/07 (QA) et du data-scout ce run (cf.
`content/inbox/runs/2026-07-27.md`, section « Incident réseau »). Le contrôle
d'originalité repose donc à nouveau uniquement sur la comparaison des champs
`title`/`note` avec les `claim` de l'inbox (déjà reformulés une fois par le
data-scout via `WebSearch`), pas sur le texte intégral des pages sources. Pour
objectiver le sondage, j'ai mesuré un ratio de similarité de caractères
(`difflib.SequenceMatcher`) et la plus longue sous-séquence commune entre
chaque `note.fr` et son `claim` d'origine — méthode identique à celle utilisée
le 21/07 sur `poi_downtown_business_district`.

---

## Verdicts par fichier

### `content/poi/poi_southern_archipelago.json` — **PASS**
- Schéma : conforme (validation `jsonschema` OK — `id` matche le pattern,
  `category: landmark` ∈ enum, `position: null` autorisé, `title`/`note` avec
  `en`+`fr`, `status: draft`, `sources` non vide, `additionalProperties`
  respecté).
- Marques : aucun terme déposé dans `title`/`note` (recherche automatisée sur
  GTA, Grand Theft Auto, Rockstar, Vice City, Leonida, Grassrivers, Port
  Gellhorn, Ambrosia, Mount Kalaga, Leonida Keys, Take-Two, Jason, Lucia,
  Clewiston — aucune occurrence). Bon point : le nom propre « Leonida Keys »
  du `claim` source est bien évité, remplacé par « archipel de Floride ».
- Traçabilité : `sources: ["https://gta.fandom.com/wiki/Leonida_Keys"]`
  correspond exactement au `source_url` du fait `poi` correspondant.
- Localisation : `en` + `fr` présents pour `title` et `note`.
- Originalité : ratio de similarité mesuré à 0.073 ; plus longue séquence
  commune = « sud de l'État » (14 caractères), un connecteur factuel
  peu significatif. Reformulation solide (« porte d'entrée décontractée » →
  ambiance, boutiques de plongée, rythme au ralenti ; référence Floride
  généralisée sans nommer « Keys »). Aucune quasi-copie détectée.

### `content/poi/poi_flooded_marshlands.json` — **PASS**
- Schéma : conforme (idem ci-dessus).
- Marques : aucun terme déposé.
- Traçabilité : `sources: ["https://gta.fandom.com/wiki/Grassrivers"]` conforme
  au fait source.
- Localisation : `en` + `fr` présents.
- Originalité : ratio 0.040 ; plus longue séquence commune insignifiante
  (« ig », 2 caractères). Point de vigilance mineur : le mot « prairies »
  est repris du `claim » (« prairies inondées » → « Prairies noyées »), et la
  formule « en écho direct aux Everglades réels de Floride » → « La référence
  aux vrais Everglades de Floride saute aux yeux » reste une paraphrase assez
  proche sur ce segment précis (4 mots de contenu partagés : Everglades,
  réels/vrais, de, Floride). Le reste de la note (canaux, cyprès, airboats,
  alligators, humidité) est un apport éditorial original substantiel. Je ne
  bloque pas le fichier mais signale le motif — voir section « à trancher ».

### `content/poi/poi_faded_port_town.json` — **PASS**
- Schéma : conforme.
- Marques : aucun terme déposé (ni « Port Gellhorn » ni « Kelly County » dans
  `title`/`note`).
- Traçabilité : `sources: ["https://gta.fandom.com/wiki/Port_Gellhorn"]`
  conforme.
- Localisation : `en` + `fr` présents.
- Originalité : ratio 0.088 ; plus longue séquence commune = « aujourd'hui »
  isolé dans son contexte. Point de vigilance mineur : « une ancienne
  destination de vacances » (claim) → « Ancienne halte de vacances » (note) —
  seul « destination » est remplacé par « halte », le reste de la structure
  (« ancienne … de vacances ») est identique. Le reste de la note (entrepôts
  qui rouillent, devantures à moitié fermées, routes de campagne, mélancolie)
  est bien reformulé et étoffé. Ne bloque pas le fichier mais motif similaire
  au cas ci-dessus — noté en section « à trancher ».

### `content/poi/poi_lakeside_refinery_town.json` — **PASS (cas le plus limite du lot)**
- Schéma : conforme — 3 sources fusionnées (`Ambrosia`, `Ambrosia_County`,
  `Allied_Crystal_Sugar_Refinery`), toutes correctement rattachées au même
  fait `poi` de l'inbox (recoupement légitime, `sources` non vide).
- Marques : aucun terme déposé — en particulier « Ambrosia » et « Clewiston »
  (nom de la vraie ville d'inspiration citée dans le `claim`) sont absents de
  `title`/`note`, bon réflexe d'anonymisation.
- Localisation : `en` + `fr` présents.
- Originalité : ratio le plus élevé du lot, **0.143** ; plus longue séquence
  commune = « e raffinerie de sucre » (22 caractères, ~4 mots). Comparaison
  de la clause structurante :
  - `claim` : « dont l'économie repose largement sur une raffinerie de sucre
    fictive exploitée par une entreprise agricole locale »
  - `note.fr` : « dont toute l'économie tient à une immense raffinerie de
    sucre »
  Le connecteur « dont [toute] l'économie [repose largement sur / tient à]
  une [immense] raffinerie de sucre » reprend un gabarit de phrase très
  proche du `claim`, avec principalement des synonymes insérés plutôt qu'une
  reformulation structurelle. Cela dit, contrairement aux cas FAIL du 21/07
  (`poi_international_airport`, `poi_arts_center`, 8-9 mots consécutifs
  identiques sur toute une proposition), la séquence identique la plus longue
  ici ne dépasse pas 4 mots (« raffinerie de sucre »), qui est un label
  technique difficilement reformulable sans perdre le sens factuel (comme
  « salle de spectacle/arène » le 21/07). Le reste de la note (cheminées,
  champs plats, ambiance ville-usine, salaires) est un apport propre. Je ne
  bloque pas le fichier sur cette seule base, mais c'est le cas le plus
  proche du seuil de FAIL de tout le lot — à surveiller en priorité si un
  humain doit arbitrer, cf. section « à trancher ».

### `content/poi/poi_northern_mountain_wilderness.json` — **PASS**
- Schéma : conforme (`category: activity` ∈ enum — cohérent avec le contenu
  chasse/pêche/pistes tout-terrain).
- Marques : aucun terme déposé (« Mount Kalaga » absent de `title`/`note`).
- Traçabilité : `sources` conforme au `source_url` du fait.
- Localisation : `en` + `fr` présents.
- Originalité : ratio 0.034, le plus bas du lot ; plus longue séquence commune
  insignifiante (« à », isolé). Bonne reformulation, y compris le contraste
  original ajouté « Si le sud brille au néon, ici on troque les néons contre
  les pins et la terre battue », absent du `claim`.

---

## Faits `news`/`cheat` non traités (5 faits de `2026-07-27-gta6-news-cheats.facts.json`)

Aucun draft n'a été produit à partir de ce fichier (trailer 3, date d'earnings
call Take-Two, dépôt SEC des prévisions de revenus, projections de
précommandes par des analystes, statut des cheats au 16/07). **C'est noté et
justifié**, pas silencieusement oublié : le message du commit `e8f33ce`
indique explicitement « faits actu laissés non traités (aucun schéma
content/ ne correspond) ». Deux réserves cependant :
1. Contrairement au fichier `2026-07-27-gta6-map-regions.facts.json` où chaque
   fait porte un champ `"processed": true`, les 5 faits de
   `2026-07-27-gta6-news-cheats.facts.json` **n'ont aucun champ `processed`**
   (ni `true` ni `false` avec motif). La justification n'existe donc que dans
   le message de commit git, pas dans l'inbox elle-même — moins traçable/
   auditable si quelqu'un consulte uniquement le JSON. Voir « à trancher ».
2. Le fait `cheat` (confidence `rumor`, « aucune liste de codes officielle
   publiée, code d'argent illimité jugé peu probable ») est correctement
   resté non promu : `confidence: rumor` ne doit jamais devenir un draft
   publiable (règle 6), et de toute façon aucun cheat candidat n'atteindrait
   `verifiedBy >= 2`. Cohérent avec le 21/07.

---

## Cheats

Aucun fichier `content/cheats/*.json` produit cette semaine — cohérent avec
l'absence de tout fait `cheat` exploitable en draft (le seul fait cheat du run
est `confidence: rumor` et ne décrit aucune séquence de touches). Recoupement
`verifiedBy >= 2` : **N/A**, rien à recouper.

---

## À trancher par un humain

1. **`poi_lakeside_refinery_town.json`** — cas le plus proche du seuil de FAIL
   originalité (ratio 0.143, gabarit de phrase « dont [toute] l'économie
   [repose sur/tient à] une raffinerie de sucre » très proche du `claim`).
   Un humain doit décider si la reformulation actuelle suffit ou si la phrase
   d'ouverture doit être retravaillée avant publication, par analogie avec
   les arbitrages `poi_concert_arena`/`poi_arts_center` du 21/07.
2. **`poi_flooded_marshlands.json`** et **`poi_faded_port_town.json`** — motifs
   mineurs de reprise sur de courts segments (« Everglades réels/vrais de
   Floride » ; « ancienne … de vacances »). Je ne bloque pas ces fichiers mais
   le pattern se répète pour la 2e semaine consécutive (déjà signalé le 21/07
   sur `poi_coastal_district`) — probable symptôme structurel du prompt de
   reformulation du data-scout plutôt que 3 incidents isolés.
3. **Absence de champ `processed` sur les 5 faits `news`/`cheat` non
   traités** — la justification n'existe que dans le message de commit git,
   pas dans `2026-07-27-gta6-news-cheats.facts.json` lui-même. Décider si le
   pipeline doit exiger un champ explicite (`"processed": false, "reason":
   "no matching schema"`) pour que la justification survive indépendamment de
   l'historique git — et si un nouveau schéma (`news.schema.json` ou
   équivalent) doit être créé pour ces faits commerciaux/calendaires
   (trailer, earnings call, dépôt SEC, précommandes), ou si c'est un choix de
   périmètre délibéré et durable du pipeline `content/` (actuellement scopé à
   poi/cheats/guides selon `content/README.md`).
4. **Positions `null` sur les 5 POI** — attendu et annoncé dans chaque `note`
   (« aucune carte officielle complète n'est encore sortie… ce point reste
   provisoire »), cohérent avec le doute n°3 de `content/inbox/runs/2026-07-27.md`
   (le découpage en six régions ne constitue pas une carte cartographique
   complète). Un humain devra placer les positions dès qu'une carte officielle
   ou une reconstruction communautaire fiable sera disponible ; jusque-là les
   5 fichiers doivent rester `status: draft`.
5. **Limitation d'accès réseau persistante** (3e run consécutif : 403 sur
   `gta.fandom.com`/`gtaboom.com`/`rockstargames.com`, et blocage structurel de
   `reddit.com` au niveau outil) — confirme qu'il s'agit d'une limitation
   durable de l'environnement, pas ponctuelle. Recommandation inchangée depuis
   le 21/07 : documenter cette limitation comme structurelle pour le pipeline
   QA (originalité vérifiée seulement au niveau `claim`, jamais au niveau
   texte source brut) plutôt que de la retester chaque semaine en espérant un
   déblocage.
6. **r/GTA6 non couvert pour la 2e semaine consécutive** (doute n°2 du
   data-scout) — hors mandat direct de QA mais impacte indirectement la
   couverture ; à trancher au niveau infra/produit (API Reddit officielle ou
   relecture humaine manuelle).

Aucune typo évidente relevée dans les 5 fichiers ; aucune correction
appliquée (hors mandat de cet agent).

---

## Résumé (3 lignes)

5/5 POI passent tous les contrôles (schéma, marques, traçabilité,
localisation, originalité) — `poi_lakeside_refinery_town` est le cas le plus
limite sur l'originalité (gabarit de phrase proche du `claim`, à surveiller)
mais reste sous le seuil de FAIL observé le 21/07. Les 5 faits `news`/`cheat`
non traités sont bien justifiés dans le commit (aucun schéma ne convient),
mais cette justification n'apparaît pas dans l'inbox elle-même — à
formaliser. Aucun problème de marque, de cheat prématuré ou de traçabilité ;
la limitation réseau (403) sur les sources externes reste structurelle,
confirmée pour la 3e semaine consécutive.
