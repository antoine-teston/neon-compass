# Veille du 2026-08-11 — second run, rebasé sur le premier

Ce compte-rendu documente un **second run de la routine survenu le même
jour**, quelques minutes après un premier (voir `2026-08-11.md` et son commit
`af1f753` sur `veille/courante`, PR #107). Les deux sessions ont tourné en
parallèle sans se voir : deux runs `recolte.yml` déclenchés à 02:17:31Z et
02:18:16Z, l'un juste après l'autre. Ce compte-rendu-ci correspond au second.

## ⚠️ Étape 4 en échec — branche de transport toujours en ligne

Confirmation indépendante de la même panne que le premier run a déjà
signalée : `veille/recolte` n'a pas pu être supprimée depuis cette session.
Trois chemins tentés, tous refusés :

1. `gh` CLI — binaire absent de l'environnement d'exécution.
2. Un outil MCP GitHub de suppression de ref/branche — recherché explicitement
   dans le jeu d'outils disponible (`actions_*`, `create_branch`, `delete_file`,
   `pull_request_*`, etc.) : aucun ne supprime une branche ou une ref.
3. `git push origin --delete veille/recolte` — `HTTP 403`.

Même limitation que celle déjà documentée le 2026-08-10 et dans le premier
run d'aujourd'hui. Pas de nouvelle information, mais deux sessions
indépendantes qui butent sur exactement la même absence d'outillage
confirment qu'il ne s'agit pas d'un accident de session : cet environnement
d'exécution n'a ni `gh`, ni son équivalent MCP.

## Récolte

`recolte.yml` déclenché séparément avec `since=2 max=15` (run `31451961372`),
quasi simultané à celui du premier run. Mêmes 6 pages rapportées, mêmes 2
URLs déjà couvertes par des runs antérieurs (08-09).

## Faits extraits par cette session — 4

Cette session a lu les 4 mêmes pages neuves que le premier run et en a extrait
4 faits (contre 3 pour le premier, qui a écarté l'anecdote de la grand-mère
comme non exploitable). Le détail des 4 faits extraits par cette session est
dans `2026-08-11b-gta6-veille.facts.json`.

## Collision détectée au push, et résolution

Au moment de pousser, `veille/courante` portait déjà le commit du premier run
(PR #107, ouverte à 02:25Z). Après rebase sur ce commit :

- **3 des 4 items rédigés par cette session dupliquaient, sur la même
  `source_url`, des items déjà matérialisés et rédigés par le premier run**
  (`news_c085b09a`, `news_2d67f3be`, `news_4ab13ef8` — respectivement les
  streamers suivis, l'« amuse-bouche » Netflix, et la monétisation). Le
  contrôle de convergence de `pull-news` ne pouvait pas les voir : chaque
  session travaillait sur son propre arbre de travail, sans le commit de
  l'autre. **Ces 3 doublons ont été retirés avant le push** — `news_92d55870`,
  `news_f1771cd9`, `news_dae4906d` supprimés de cette branche, au profit des
  versions déjà présentes et rédigées par le premier run.
- **Le 4ᵉ item est resté** : `news_bdf9c883` (anecdote de la grand-mère et de
  la console), que le premier run avait explicitement écarté comme non
  exploitable. Les deux jugements éditoriaux divergent légitimement sur un
  fait `single-source` marginal — celui-ci est conservé en `draft`, flaggé
  comme douteux (voir plus bas), pour laisser un humain trancher plutôt que
  deux sessions IA. Il ne fait doublon avec rien : le premier run n'a produit
  aucune carte pour cette URL.
- Les deux fichiers `content/inbox/2026-08-11-gta6-veille.facts.json` et
  `content/inbox/runs/2026-08-11.md` sont en collision de nom (mêmes chemins,
  contenus différents) : la version du premier run reste au chemin canonique,
  celle de cette session est renommée avec le suffixe `b`, à l'image de la
  convention déjà utilisée les 08-08 et 08-09.

## Doute laissé à un humain

`news_bdf9c883` (grand-mère achetant une console) : anecdote relayée par la
presse spécialisée depuis un message de réseau social, présentée par
l'article source lui-même comme invérifiable. Conservée en `single-source`/
`draft` avec réserve explicite dans le corps rédigé — à confirmer ou refuser
en revue humaine, sachant qu'une autre lecture indépendante du même fait l'a
jugé non exploitable.

## Garde-fous (après résolution de la collision)

- `npm test` : 489/489.
- `validate` : 684/684 OK.
- `check-publishable` : 684/684 OK.
- `check-seeds` : socles à jour.
- `check-originality` : aucune reprise littérale ni marque nue.
- `translate --dry-run` : 0 champ manquant.

Ces résultats portent sur l'état final de la branche (premier run + cette
session, doublons retirés), pas sur cette session isolée.
