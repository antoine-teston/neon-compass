# Run data-scout — 2026-08-04

## Récolte

`recolte.yml` a tourné sans incident (run 30871386264, succès) sur `main`,
avec `since=2 max=15`. Préflight vert sur les quatre domaines du registre.
Le transport est passé par la branche jetable `veille/recolte`, lue via
l'API `contents`, puis supprimée après lecture.

## Sources visitées

- `www.gtaboom.com` (flux + 6 pages)
- `leonidaverse.com` (flux ; les deux articles remontés — FR et EN — portent
  sur le même point financier du 7 août déjà couvert au run du 03/08, rien de
  neuf)

## Faits extraits — 5

| Confiance | Sujet |
|---|---|
| `confirmed-official` | Correctif du 30/07 : 13 bugs du braquage Kortz Center corrigés (mode en ligne) |
| `confirmed-official` | Hausse des prix Xbox Series X/S en Europe et au Royaume-Uni au 1er août |
| `single-source` | Lamine Yamal légende un post « GTA VI », le studio like la publication |
| `single-source` | Un fan recrée la carte estimée du jeu dans Cities: Skylines II |
| `single-source` | Le co-réalisateur de The Last of Us questionne la profondeur du jeu à venir |

Deux faits déjà en base ce run (le point financier du 7 août, la mise à jour
PS5) : la page `sony-confirms...` et les deux pages `leonidaverse.com` du
7 août n'ont produit aucun fait, déjà couverts le 03/08.

Le fichier `2026-07-30-gtav-weekly.facts.json` déposé par le runner sur
`veille/recolte` est identique octet pour octet à celui déjà présent dans
`content/inbox/` (fenêtre du 30/07 au 05/08 inchangée) — rien à recopier.

## Écarté, et pourquoi

- **Guide solo « Sewer Approach » du braquage Kortz Center en mode
  Difficile.** Même exclusion que le run du 03/08 pour les guides de
  braquage : c'est un guide pour le jeu en ligne actuel, pas un fait
  d'actualité — il relève de `content-editor`, pas de la veille.

## Ménage non fait — à signaler

La branche jetable `veille/recolte` n'a **pas pu être supprimée** ce run :
`git push origin --delete` est rejeté en 403 (la passerelle locale n'autorise
que la lecture git, pas l'écriture), et aucun outil MCP GitHub chargé cette
session ne supprime une ref (`delete_file` ne supprime qu'un fichier dans un
commit, pas une branche). La branche contient toujours `recolte.json`,
`feeds.json`, `harvest.json` et `pages/` — du texte tiers qui devait transiter,
pas séjourner. À supprimer manuellement, ou à outiller si ça se reproduit.

## À trancher par un humain

- Les trois faits `single-source` (Lamine Yamal, la carte fan, Bruce
  Straley) partent en `draft` par construction. Ce sont des couvertures
  ponctuelles d'un seul média sur des événements publics (un post Instagram,
  une carte communautaire partagée, un fil X) — vérifiables en soi, mais la
  décision de publier reste éditoriale.
- Aucune source en échec ce run, rien à signaler côté registre.
