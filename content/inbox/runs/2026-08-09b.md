# Run 2026-08-09 (session b)

Poussé après coup : au moment de pousser sur `veille/courante`, une
**autre session de cette même Routine avait déjà poussé son propre commit**
`content(veille): récolte du 2026-08-09` (fichiers `2026-08-09-…` sans
suffixe). Même principe que le 08/08 : les deux extractions restent
distinctes plutôt que fusionnées — ce fichier et
`content/inbox/2026-08-09b-gta6-veille.facts.json` portent le suffixe `b`.

Les deux sessions ont jugé les mêmes 3 URLs neuves mais divergent sur
plusieurs points :

- **La fonctionnalité de création de missions mise en avant aux
  investisseurs** : `game: leonida` ici (l'article la relie explicitement à
  la stratégie de contenu communautaire pour le jeu à venir), `game: gtav`
  côté session a (l'outil lui-même vit dans le mode en ligne actuel). Les
  deux lectures sont défendables.
- **La position anti-IA-générative du PDG** : `single-source` ici (date de
  la déclaration non précisée par la source, reprise de seconde main),
  `confirmed-official` côté session a. Un éditeur devra trancher.
- **Le fait sur les PNJ à IA générative d'un serveur de jeu de rôle tiers**
  (`gta-roleplay-npcs-can-now-talk-back…`) n'a été extrait comme fait à part
  entière que dans cette session — la session a a lu la même page mais n'en
  a gardé que la citation du PDG, pas l'existence de la modification
  elle-même. Les deux squelettes `content/news/` qui en résultent (session a
  : 2 items ; session b, celle-ci : 4 items) coexistent avec des `id`
  distincts, donc pas de collision technique. Un éditeur humain devra
  arbitrer les doublons éditoriaux (probablement 2-3 cartes proches).

## Récolte

`gh` CLI absent de cet environnement (confirmé une nouvelle fois). Déclenché
`recolte.yml --ref main -f since=2 -f max=15` via l'outil MCP GitHub
(`run_workflow`), run `31290052405`, succès. Téléchargement direct de
l'artefact `recolte` : 403 CONNECT côté passerelle de sortie
(`productionresultssa10.blob.core.windows.net` hors liste blanche) —
confirme le contournement déjà documenté dans `.claude/agents/data-scout.md` :
lecture des fichiers via `mcp__github__get_file_contents` sur la branche
jetable `veille/recolte` (API `contents`, sans redirection). Fonctionne sans
accroc.

**Le déclenchement en double s'est reproduit une cinquième journée
consécutive (05, 06, 07, 08, 09/08).** Un second run `Récolte`
(`31290049292`) a été créé à `02:18:01Z`, cinq secondes avant le mien
(`31290052405`, `02:18:06Z`), même `head_sha`, même déclencheur — signe
qu'une autre session de cette Routine a appelé `run_workflow` en parallèle,
exactement le diagnostic déjà posé les 06, 07 et 08/08. Aucune PR de veille
n'était encore ouverte au moment où j'ai vérifié : soit l'autre session n'a
pas atteint l'étape de publication, soit son extraction diffère de la
mienne. Pas de commit à fusionner à l'aveugle si la sienne apparaît après
coup — même principe que le 08/08 : les deux extractions resteront
distinctes plutôt que fusionnées.

11 pages `fetched` par le harvest (`harvest.json`), `skipped` vide, toutes
sur `www.gtaboom.com` (`since=2` ne remonte qu'aux 07-08/08). Aucun fichier
`*-gtav-weekly.facts.json` neuf : celui présent dans la récolte
(`2026-07-30-gtav-weekly.facts.json`) est identique à l'octet près à
`content/inbox/2026-07-30-gtav-weekly.facts.json` déjà en place — pas
recopié. `weekly.json` confirme l'absence de bonus hebdomadaire actif
(« sans-semaine »).

Sur les 11 URLs, 8 avaient déjà été traitées les 08/08 et 08/08b — non
relues. Restaient 3 URLs neuves, toutes du 08/08 :

- `take-two-hints-at-gta-online-tool-that-could-be-a-gta-6-preview-0a4d`
- `gta-roleplay-npcs-can-now-talk-back-remember-you-and-gossip-about-you-3d1c`
- `why-rockstar-games-continued-gta-6-online-silence-is-not-bad-news-ac01`

J'ai relu les `content/inbox/*.facts.json` existants avant d'écrire ; aucune
des trois URLs neuves n'y apparaissait déjà.

## Faits extraits — 4

| Confiance | Kind | Jeu | Sujet |
|---|---|---|---|
| `confirmed-official` | `news` | `leonida` | Documents investisseurs : l'outil de création de missions du mode en ligne actuel (ajouté 12/2025) mis en avant comme preuve d'importance du contenu généré par les joueurs, sans promesse pour le jeu à venir |
| `single-source` | `news` | `gtav` | Une société tierce ajoute dialogues générés et mémoire aux PNJ d'un grand serveur de jeu de rôle communautaire du mode en ligne actuel — modification tierce, sans lien avec le studio |
| `single-source` | `news` | `leonida` | Le PDG de l'éditeur a réaffirmé (date précise non donnée par la source) qu'aucune IA générative ni dialogue généré ne serait utilisé dans le jeu à venir |
| `single-source` | `news` | `leonida` | Un journaliste (Bloomberg) confirme l'absence de retard (toujours 19/11/2026), précise qu'aucune date n'a été donnée pour le prochain mode en ligne, et rappelle qu'en 2013 le mode en ligne de l'épisode précédent était arrivé ~2 semaines après le jeu principal |

## Écarté, et pourquoi

Rien d'écarté cette fois : les 3 pages neuves ont chacune produit au moins
un fait. Le troisième fait (position sur l'IA générative) et le quatrième
(citation Bloomberg) proviennent tous deux de pages déjà comptées pour un
autre fait — pas de doublon, deux informations distinctes dans le même
article.

## Doute à trancher par un humain

- **Le fait sur la position anti-IA-générative n'a pas de date précise**
  (« plus tôt cette année », non sourcé par gtaboom au-delà de la
  déclaration originale du PDG). Je l'ai gardé en `single-source` plutôt que
  `confirmed-official` pour cette raison — à re-sourcer directement si un
  éditeur veut remonter à la déclaration d'origine.
- **Double déclenchement récurrent (5 jours de suite).** Le signalement
  répété depuis la PR #58 n'a produit aucune correction visible côté
  scheduler/Routine. Je documente à nouveau plutôt que de re-signaler une
  sixième fois sans autre option côté outillage de cette session.

## Matérialisation, rédaction, garde-fous

Voir le corps de la PR pour l'état de `pull-news`, `pull-online-events`,
`npm test` et le résultat final.
