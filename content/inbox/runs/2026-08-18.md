# Veille — 2026-08-18

## Urgent — la branche de transport `veille/recolte` n'a pas pu être supprimée

Même limitation que les 14 à 17/08 : cette session n'a ni le binaire `gh`
(absent de l'environnement — `which gh` échoue), ni d'accès en écriture
directe à l'API GitHub, ni d'outil `mcp__github__*` supprimant une
branche/ref (`create_branch` existe, son inverse non — revérifié dans le jeu
d'outils avant d'écrire cette note, aucun `delete_branch`/`delete_ref`). La
suppression a donc été **tentée puis abandonnée**, pas oubliée. `veille/recolte`
reste exposée sur le dépôt public avec le texte intégral de la page tierce lue
ce run jusqu'à ce que `recolte-nettoyage.yml` (filet automatique, >2h) l'efface,
ou qu'un humain lance `git push origin --delete veille/recolte`.

## Récolte

`recolte.yml` déclenché sur `main` via `mcp__github__actions_run_trigger`
(`since=2`, `max=15`) — run [32091400790](https://github.com/antoine-teston/neon-compass/actions/runs/32091400790),
conclusion `success`. Récupéré via `mcp__github__get_file_contents`
(équivalent à `gh api ... -H "Accept: application/vnd.github.raw"`), copié
dans `/tmp/recolte.json` — pas de `gh` disponible dans cette session.

`feeds` : 30 titres `gtaboom.com`, 2 `leonidaverse.com` (FR/EN du même
article, déjà couvert le 08-08), 0 `gta6.gg`. `fetched` : 1 page :

- `a-gta-6-fan-is-racing-to-see-it-before-he-loses-his-sight-8e84` (16/08) —
  déjà extrait le 17/08 (`content/inbox/2026-08-17-gta6-veille.facts.json`,
  déjà `processed: true`) — vérifié par recherche d'URL avant lecture, aucun
  second fait écrit.

`skipped` : vide. Aucun `*-gtav-weekly.facts.json` dans cette récolte.

## Faits extraits

Aucun. Le seul article récolté est déjà couvert, et les deux entrées
`leonidaverse.com` (même sujet, l'Extended Look Netflix) le sont aussi depuis
le 08-08. Journée sans actualité neuve — normal en quotidien, le préflight de
`recolte.yml` ayant déjà prouvé que les sources répondaient.

## Mode en ligne

`weekly.json` (fichier séparé sur la branche de transport) rapporte
`verdict: "page-meconnaissable"` — `message: "page de hub non reconnue —
ancres manquantes : id=\"weekly-updates-heading\""`. Contrairement aux runs du
15-17/08 (verdicts hors des quatre valeurs documentées), celui-ci EST l'une
des quatre de `.github/pr-body-veille.md` : « Les ancres de la page ont bougé :
refonte côté source. À traiter. » — donc probablement une vraie refonte du hub
source, pas un artefact de l'outil. `pull-online-events` confirme : rien de
neuf (1 déjà à jour).

## Matérialisation

`npm ci` puis `pull-news` : 0 squelette écrit, 7 écartés mécaniquement, 95
déjà présents. `pull-online-events` : rien de neuf (1 déjà à jour).

### Écartés à la matérialisation (contrôle mécanique, pas mon jugement)

Sept URLs de faits extraits les jours précédents, capturées au passage par
`pull-news` sur l'ensemble de `content/inbox/` — aucune ne vient de la
récolte lue ce run (voir §Récolte, 1 page seulement) :

- `what-to-expect-when-gta-6-hits-netflix-on-august-27-c2f7` → déjà porté par
  `news_285eebb3` — « Un message du support client de Netflix, relayé sans
  confirmation officielle par la presse spécialis… »
- `rockstar-games-quietly-followed-a-small-army-of-streamers-888d` → déjà
  porté par `news_c085b09a` — « Le compte Instagram officiel du studio a
  discrètement suivi six streameurs de premier plan quelques … »
- `gta-6-netflix-reveal-only-an-appetizer-zelnick-tease-dbed` → déjà porté par
  `news_2d67f3be` — « Le PDG de l'éditeur, interrogé par un média spécialisé, a
  qualifié la diffusion du 27 août de simple… »
- `gta-6-is-take-twos-test-for-monetizing-everyone-9597` → déjà porté par
  `news_4ab13ef8` — « Le PDG de l'éditeur a déclaré à un média spécialisé que
  l'industrie du jeu vidéo n'exploite encore q… »
- `78-year-old-grandmother-buys-xbox-series-x-for-gta-6-8dd9` → refus du
  2026-08-17, déjà porté par `news_bdf9c883` — « Un internaute affirme sur un
  réseau social que sa grand-mère de 78 ans a acheté une console de nouve… »
- `gta-6-pre-orders-skew-toward-100-ultimate-edition-as-zelnick-defends-pricing-3bba`
  → déjà porté par `news_83783999` — « Interrogé par CNBC, le PDG de l'éditeur
  affirme que les précommandes du jeu à venir penchent davanta… »
- `zelnick-says-gta-6-will-exceed-expectations-f366` → déjà porté par
  `news_a3b96f40` — « Toujours interrogé par CNBC, le PDG de l'éditeur affirme
  croire que le studio dépassera les attentes… »

## Rédaction

Rien à rédiger : aucun `content/news/*.json` ne porte `needsRewrite: true`.

## Garde-fous

- `npm test` (le mur) : 514/514 tests passés.
- `npm run check` complet vert : `validate` (696/696 OK), `check-publishable`
  (696/696 OK), `check-seeds` (socles à jour), `check-originality` (72 effets
  de cheat, 31 champs d'événement en ligne, aucune reprise littérale).
- `node cli.js translate --dry-run` : **0 champ manquant** — rien à rattraper.

## Doutes pour un humain

- La branche `veille/recolte` non supprimable : cinquième jour d'affilée (14
  à 18/08) où la session ne dispose d'aucun moyen de la nettoyer. Vaut la
  peine de trancher l'architecture (donner un outil de suppression de ref au
  serveur MCP, ou un accès `gh` authentifié) plutôt que de compter sur le
  filet à chaque run.
- `weekly.json` signale `page-meconnaissable` pour la première fois avec ce
  libellé exact (les 15-17/08 rapportaient des verdicts hors nomenclature) :
  vaut la peine de vérifier si `weekly-hub.mjs` doit être mis à jour pour
  suivre une refonte du hub GTA Online côté source.
- `git status --porcelain content/` est vide (aucune actu neuve, aucune
  réécriture), mais `pull-news` a écarté 7 entrées mécaniquement : ce
  compte-rendu se commite seul, conformément au contrat.
