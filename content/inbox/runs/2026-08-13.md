# Veille — 2026-08-13

## Récolte

`recolte.yml` déclenché sur `main` (`since=2`, `max=15`), run [31660370748](https://github.com/antoine-teston/neon-compass/actions/runs/31660370748), conclusion `success`.

**Note process** : le téléchargement de l'artefact `recolte` via l'API GitHub redirige vers `productionresultssa15.blob.core.windows.net`, que la passerelle de cette session refuse au CONNECT (403, confirmé par `$HTTPS_PROXY/__agentproxy/status`). C'est un blocage différent des quatre domaines du registre — celui-ci porte sur le CDN Azure d'Actions lui-même. Le workflow `recolte.yml` documente déjà ce contournement dans ses commentaires : la récolte est aussi déposée sur la branche jetable `veille/recolte`, lue via l'API `contents` (`api.github.com`, seul hôte joignable depuis cette session). C'est par ce chemin que la récolte du jour a été récupérée — pas par `gh run download`, qui échouerait de la même façon.

## Sources visitées

- `www.gtaboom.com/feed.xml`
- `leonidaverse.com/news-sitemap.xml`
- 5 pages rapportées par `harvest.json` (fenêtre 2 jours) :
  - `zelnick-warns-gta-6-fans-not-to-wait-for-a-holiday-price-drop-b05b` (12/08) — **nouveau**
  - `gta-6-pre-orders-skew-toward-100-ultimate-edition-as-zelnick-defends-pricing-3bba` (11/08) — déjà extrait le 12/08 (deux fois)
  - `zelnick-says-gta-6-will-exceed-expectations-f366` (11/08) — déjà extrait le 12/08 (deux fois)
  - `gta-6-fans-raised-50k-so-this-man-could-live-to-november-19-9f2c` (11/08) — déjà extrait le 12/08
  - `kai-cenat-wants-to-100-gta-6-in-one-nonstop-marathon-d0ed` (11/08) — déjà extrait le 12/08

## Faits extraits

1 fait, `content/inbox/2026-08-13-gta6-veille.facts.json` : le PDG de l'éditeur confirme sur une chaîne financière américaine qu'il n'y aura pas de remise de fin d'année sur le jeu à venir.

## Écarté

Les 4 autres pages de la fenêtre `--since 2` portent sur des articles déjà couverts par les runs du 12/08 (même URL, même `source_date` du 11/08) — vérifié par recherche dans `content/inbox/*.facts.json` avant extraction. Pas de nouveau fait pour éviter le doublon que la chaîne écarterait de toute façon (cf. note dans `data-scout.md` sur les extractions redondantes des 6-7/08).

## Mode en ligne

`weekly.json` rapporte `verdict: "page-meconnaissable"` — ancre `aria-label="Weekly event sections"` introuvable sur le hub. Ce n'est **pas** `sans-semaine` (source sans semaine publiée) : la page du hub a changé de structure et l'extracteur ne la reconnaît plus. Le HTML capturé (`hub.html`, 414 Ko) est sur `veille/recolte` pour reancrer l'extracteur. **Doute à trancher par un humain** : `fetch-source.mjs weekly` a besoin d'une mise à jour de ses ancres — pas un problème de contenu, un problème d'extracteur.

## Suite

Rien à publier via `pull-online-events` aujourd'hui (pas de `*-gtav-weekly.facts.json`). `pull-news` s'applique au fait unique ci-dessus.
