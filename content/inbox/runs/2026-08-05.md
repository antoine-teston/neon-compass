# Run data-scout — 2026-08-05

**Note liminaire : deux sessions de cette Routine ont tourné en parallèle ce
jour** (`session_01YRTnpENZncCWWDAyfCZ48d` puis celle-ci), à quelques minutes
d'écart, chacune déclenchant son propre `recolte.yml` et écrivant sur
`veille/courante` sans connaître l'autre. Les deux poussées ciblaient le même
chemin `content/inbox/2026-08-05-gta6-veille.facts.json` : la seconde a
silencieusement écrasé le fait de la première (aucun outil MCP GitHub chargé
ici n'expose de fusion de fichiers, seulement create-or-update par chemin).
Ce fichier et ce compte-rendu **reconstituent les deux extractions** plutôt
que de n'en garder qu'une — voir « À trancher par un humain » pour la
question de fond que ça pose.

## Récolte

Deux runs de `recolte.yml` sur `main`, `since=2 max=15` :

- run `30968940645` (session parallèle), succès ;
- run `30969026004` (cette session), succès.

Même fenêtre, même registre de sources, préflight vert sur les quatre
domaines à chaque fois. Chaque run a lu sa propre copie de `recolte.json` sur
la branche jetable `veille/recolte`, via l'API `contents`.

## Sources visitées (communes aux deux runs)

- `www.gtaboom.com` (flux + 2 pages neuves : Lewis Hamilton daté du 04/08,
  guide solo Kortz Center)
- `leonidaverse.com` (flux ; rien de neuf depuis le point financier du 7 août,
  déjà couvert le 03/08)
- Les six autres titres du flux `gtaboom.com` (Sony PS5, hausse des prix
  Xbox, Lamine Yamal, la carte fan Cities: Skylines II, le co-réalisateur de
  The Last of Us, le correctif du braquage Kortz Center) sont déjà en base,
  extraits aux runs du 03/08 et du 04/08 — aucun fait dupliqué.

## Faits extraits — 2 (un par run, jugements éditoriaux divergents)

| Confiance | Kind | Sujet |
|---|---|---|
| `single-source` | `game-fact` | Parcours solo Difficile du braquage Kortz Center par les égouts, sous 15 minutes, quatre conditions du défi Élite remplies |
| `single-source` | `news` | Un clip viral de Lewis Hamilton sur le jeu date en fait de juillet 2025 et refait surface faute d'actualité ; deux entreprises confirment fermer ou réduire leurs effectifs le jour de la sortie |

Les deux runs ont tranché en sens inverse sur les deux mêmes pages :

- **Guide solo « Sewer Approach ».** La session parallèle l'a retenu comme
  `game-fact` — une performance datée et vérifiable (temps, conditions du
  défi Élite), distincte d'un guide pas-à-pas complet. Cette session l'avait
  d'abord écarté par réflexe de cohérence avec le run du 04/08 (qui avait
  écarté un guide similaire), sans distinguer qu'un fait ponctuel sur une
  performance n'est pas la même chose qu'un guide entier. Le jugement de la
  session parallèle est retenu ici.
- **Article Lewis Hamilton.** Cette session l'a retenu comme `news`
  (single-source, `draft`) pour le détail vérifiable et non lié à la citation
  recyclée : deux entreprises qui ferment ou réduisent leurs effectifs le
  jour de la sortie. La session parallèle l'a écarté en jugeant qu'il n'y
  avait « rien de neuf sur le jeu », lecture qui écartait ce même détail.
  Le fait est conservé ici — l'éditeur humain reste juge en dernier ressort
  via le statut `draft`.

Le fichier `2026-07-30-gtav-weekly.facts.json` déposé par le runner sur
`veille/recolte` est, dans les deux runs, identique octet pour octet à celui
déjà présent dans `content/inbox/` (fenêtre du 30/07 au 05/08 inchangée,
dernier jour avant rotation) — rien à recopier.

## Matérialisation

Seul le fait `news` passe par `pull-news` (`content/news/news_635a6222.json`,
`draft`, rédigé en FR/EN). Le fait `game-fact` n'est consommé ni par
`pull-news` ni par `pull-online-events` — il attend `content-editor`, comme
prévu par le registre des kinds.

## Ménage non fait — à signaler (récurrent, 3e run de suite)

`veille/recolte` n'a, à nouveau, pas pu être supprimée dans cette session :
`git push origin --delete` renvoie 403, et aucun outil MCP GitHub chargé ici
ne supprime une ref (seuls `create_branch` et `delete_file` existent, aucun
`delete_ref`/`delete_branch`). La session parallèle rapporte le même blocage
et note en plus une branche `_push-test-scratch` (vide, créée pour isoler le
problème) qui traîne également sur le dépôt. Les deux branches sont à
supprimer manuellement, ou à outiller côté registre MCP si ça continue de se
reproduire.

## À trancher par un humain

- **Le déclenchement en double de cette Routine le même jour** est le point
  le plus important de ce compte-rendu : deux sessions ont tourné à quelques
  minutes d'écart sans se coordonner, et n'importe quelle collision de nom de
  fichier entre deux exécutions concurrentes se résout aujourd'hui par un
  écrasement silencieux plutôt qu'un conflit visible. Vérifier la
  configuration de planification qui a permis ce double déclenchement.
- Les deux faits partent en `draft` par construction (`single-source`) — au
  choix de l'éditeur de publier ou non.
- Aucune source en échec ce run, rien à signaler côté registre.
