# Veille — 2026-08-17

## Urgent — la branche de transport `veille/recolte` n'a pas pu être supprimée

Même limitation que les 14 à 16/08, toujours vraie aujourd'hui : cette session
n'a ni le binaire `gh` (absent de l'environnement — `which gh` échoue), ni
d'accès en écriture directe à l'API GitHub (`curl -X DELETE
.../git/refs/heads/veille/recolte` renvoie `403 "Write access to this GitHub
API path is not permitted through this proxy"` — les lectures directes à
`api.github.com` passent, les écritures non), ni d'outil `mcp__github__*`
supprimant une branche/ref (`create_branch` existe, son inverse non —
revérifié dans le jeu d'outils avant d'écrire cette note). La suppression a
donc été **tentée puis abandonnée**, pas oubliée. `veille/recolte` reste
exposée sur le dépôt public avec le texte intégral des trois pages tierces
lues ce run jusqu'à ce que `recolte-nettoyage.yml` (filet automatique, >2h)
l'efface, ou qu'un humain lance `git push origin --delete veille/recolte`.

## Récolte

`recolte.yml` déclenché sur `main` via `mcp__github__actions_run_trigger`
(`since=2`, `max=15`) — run [31987607639](https://github.com/antoine-teston/neon-compass/actions/runs/31987607639),
conclusion `success`. Récupéré via l'API `contents` (lecture directe HTTPS vers
`api.github.com`, équivalente à `gh api ... -H "Accept: application/vnd.github.raw"`,
copiée dans `/tmp/recolte.json`) — pas de `gh` disponible dans cette session.

`feeds` : 30 titres `gtaboom.com`, 2 `leonidaverse.com` (FR/EN du même
article, déjà couvert), 0 `gta6.gg`. `fetched` : 3 pages :

- `a-gta-6-fan-is-racing-to-see-it-before-he-loses-his-sight-8e84` (16/08) — nouveau
- `nopixel-v-hits-the-official-rockstar-games-launcher-on-september-8-2df1` (15/08) — déjà extrait le 16/08
- `the-first-real-bully-successor-just-crashed-and-burned-ac4c` (15/08) — déjà écarté le 16/08 (même motif qu'aujourd'hui, voir plus bas)

`skipped` : vide. `content/inbox/refus.json` n'existe toujours pas dans ce
dépôt. Aucun `*-gtav-weekly.facts.json` dans cette récolte.

## Faits extraits

1 fait dans `content/inbox/2026-08-17-gta6-veille.facts.json` :

1. Un joueur légalement aveugle depuis l'enfance, dont la vue continue de se
   dégrader, espère terminer le jeu à venir au moins une fois avant de perdre
   totalement la vue — `single-source`, `game: leonida`.

## Écarté (jugement de lecture, étape 3)

- `the-first-real-bully-successor-just-crashed-and-burned-ac4c` : critique
  d'un jeu tiers sans rapport (un « successeur spirituel » d'un studio
  indépendant), utilisée comme argument éditorial sur la stratégie
  mono-franchise de l'éditeur. Aucune annonce factuelle sur le jeu à venir ni
  sur le mode en ligne actuel — pure spéculation/éditorial sur un produit d'un
  tiers non lié. Pas de fait émis plutôt que de forcer un rattachement à
  `leonida` ou `gtav`. Même verdict que le run du 16/08 sur le même article.
- `nopixel-v-hits-the-official-rockstar-games-launcher-on-september-8-2df1` :
  déjà extrait le 16/08 (`content/inbox/2026-08-16-gta6-veille.facts.json`) —
  vérifié par recherche d'URL avant lecture, pas de second fait écrit.

## Mode en ligne

`weekly.json` (fichier séparé sur la branche de transport, à côté de
`recolte.json` — vérifié après coup via l'API `contents`) rapporte
`verdict: "sans-structure"` : une actu hebdomadaire est repérée
(`gta-online-brand-wars-event-august-2026`), déjà couverte par les faits 6 et 7
du run du 15/08, mais pas de tableau de bonus structuré à en extraire. Ce
verdict ne correspond à aucune des quatre valeurs documentées dans
`.github/pr-body-veille.md` (`sans-semaine`, `declaration-inconnue`,
`page-meconnaissable`, `payload-absent`/`hub-injoignable`) — même situation que
le run du 16/08, signalé ici plutôt que forcé dans une des quatre cases.
`2026-07-30-gtav-weekly.facts.json`, toujours présent sur la branche de
transport, est un résidu d'un run bien antérieur (déjà `processed: true`, déjà
présent dans `content/inbox/` depuis le 10/08) — rien de nouveau à en copier.
`pull-online-events` confirme : rien de neuf (1 déjà à jour).

## Matérialisation

`npm ci` puis `pull-news` : 1 squelette écrit (`content/news/news_8ce0c2c6.json`,
depuis le fait ci-dessus), 6 écartés mécaniquement, 95 déjà présents.
`pull-online-events` : rien de neuf (1 déjà à jour).

### Écartés à la matérialisation (contrôle mécanique, pas mon jugement)

Six URLs de faits extraits les jours précédents, dont la `source_url` est déjà
portée par une entrée existante — reformulées différemment mais rejetées
malgré tout, comme prévu. Ces six ne viennent pas de la récolte lue ce run
(voir §Récolte, 3 pages seulement) : ce sont des faits extraits par des runs
antérieurs qui n'avaient pas encore été matérialisés, capturés au passage par
`pull-news` sur l'ensemble de `content/inbox/` :

- `what-to-expect-when-gta-6-hits-netflix-on-august-27-c2f7` → déjà porté par
  `news_285eebb3` — « Un message du support client de Netflix, relayé sans
  confirmation officielle par la presse spécialis… »
- `rockstar-games-quietly-followed-a-small-army-of-streamers-888d` → déjà
  porté par `news_c085b09a` — « Le compte Instagram officiel du studio a
  discrètement suivi six streameurs de premier plan quelques … »
- `gta-6-netflix-reveal-only-an-appetizer-zelnick-tease-dbed` → déjà porté par
  `news_2d67f3be` — « Le PDG de l'éditeur, interrogé par un média spécialisé, a
  qualifié la diffusion du 27 août de simple… »
- `gta-6-is-take-twos-test-for-monetizing-everyone-9597` → déjà porté par
  `news_4ab13ef8` — « Le PDG de l'éditeur a déclaré à un média spécialisé que
  l'industrie du jeu vidéo n'exploite encore q… »
- `gta-6-pre-orders-skew-toward-100-ultimate-edition-as-zelnick-defends-pricing-3bba`
  → déjà porté par `news_83783999` — « Interrogé par CNBC, le PDG de l'éditeur
  affirme que les précommandes du jeu à venir penchent davanta… »
- `zelnick-says-gta-6-will-exceed-expectations-f366` → déjà porté par
  `news_a3b96f40` — « Toujours interrogé par CNBC, le PDG de l'éditeur affirme
  croire que le studio dépassera les attentes… »

## Rédaction

`news_8ce0c2c6.json` rédigé dans les cinq langues (`en`, `fr`, `es`, `it`,
`de`) depuis `sourceClaim`, `category: announcement` conservé, `status` resté
`draft` (`confidence: single-source`). `needsRewrite` retiré.

## Garde-fous

- `node cli.js validate` : 697/697 OK.
- `node cli.js check-publishable` : 697/697 OK.
- `npm test` (le mur) : 505/505 tests passés, `npm run check` complet vert
  (`validate`, `check-publishable`, `check-seeds`, `check-originality`).
- `node cli.js translate --dry-run` : **0 champ manquant** — rien à rattraper.

## Doutes pour un humain

- La branche `veille/recolte` non supprimable : quatrième jour d'affilée (14,
  15, 16, 17/08) où la session ne dispose d'aucun moyen de la nettoyer. Vaut la
  peine de trancher l'architecture (donner un outil de suppression de ref au
  serveur MCP, ou un accès `gh` authentifié) plutôt que de compter sur le
  filet à chaque run.
